minio:
  type: minio
  url: http://192.168.1.118:9000
  accessKey: minioadmin
  secretKey: minioadmin
  timeout: 20000
  secure: false                    # 是否启用HTTPS
  bucketName: szmtr
  connect-timeout: 5000            # 连接超时(ms)
  write-timeout: 60000             # 写入超时(ms)
  expirDate: 7    #有效天数

file:
  path: d:/upload  #上传文件存储在本地的根路径
  prefix: minio #资源映射路径 前缀